[build-system]
requires = ["setuptools>=65.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pecs_pro"
version = "0.1.0"
description = "PECS-PRO workspace continuity cache and daemon"
authors = [
  {name = "PECS-PRO"}
]
readme = "README.md"
requires-python = ">=3.9"

[project.urls]
Home = "https://example.com/pecs_pro"